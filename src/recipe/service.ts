import {Recipe} from "./entity";

export class RecipeService {
    findById(id: string) {

    }

    findAll(query: any) {

    }

    addNew(entity: any): Promise<Recipe> {
        return new Promise(() => {});
    }

    removeById(id: string) {

    }
}
